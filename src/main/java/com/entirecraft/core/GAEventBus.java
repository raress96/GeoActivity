package com.entirecraft.core;


public class GAEventBus 
{
	/*
	@SubscribeEvent()
	public void entityAttackEvent(LivingAttackEvent event)
	{
		System.out.println("works1");
		if(event.source.getEntity() instanceof EntityLivingBase)
		{
			if(((EntityLivingBase) event.source.getEntity()).getHeldItem()!=null)
			{
				int j = EnchantmentHelper.getKnockbackModifier((EntityLivingBase) event.source.getEntity(), event.entityLiving);
				if(j>0)
				{
					for(int i=1;i<=4;i++)
						if(event.entityLiving.getEquipmentInSlot(i)!=null && event.entityLiving.getEquipmentInSlot(i).getItem() instanceof AdvancedArmor)
						{
							//if(((EntityLiving) event.target).getEquipmentInSlot(i).getTagCompound().getBoolean("knockback"))
							
							break;
								
						}
				
				}
			}
		}
	}
	*/
}
